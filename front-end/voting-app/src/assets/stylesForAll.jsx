import { StyleSheet } from "react-native";


const stylesForAll = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#222D32',
      },
});

export default stylesForAll;